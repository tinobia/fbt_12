<% provide :title, t(".title") %>
<% content_for :action do %>
  <%= link_to t("shared.back"), :back, class: "btn btn-dark" %>
<% end %>
<div class="row">
  <div class="col mx-auto bg-light m-2 pt-4">
    <h2 class="text-center"><%= t "admin.categories.show.category_informations" %></h2>
    <div class="mx-auto">
      <%= render "form", submit_text: t("shared.save_changes"),
        form_url: admin_category_path(@category) %>
    </div>
  </div>
</div>
