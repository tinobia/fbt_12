<!DOCTYPE html>
<html>
  <head>
    <title><%= full_title yield(:title) %></title>
    <%= csrf_meta_tags %>

    <%= stylesheet_link_tag "application", media: "all", "data-turbolinks-track": "reload" %>
  </head>

  <body>
    <%= render "admin/header" %>
    <%= render "admin/title_bar"%>
    <div class="container mt-0">
      <%= render "shared/errors" %>
      <%= yield %>
    </div>
  </body>
</html>
